query LaunchesPastList($find: LaunchFind, $limit: Int, $offset: Int) {
  launchesPast(find: $find, limit: $limit, offset: $offset) {
		id
    details
    launch_date_local
   rocket{
    rocket_name
  }
		mission_name
  }
}