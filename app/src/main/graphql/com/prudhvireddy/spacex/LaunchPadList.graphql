query LaunchPadList($offset: Int, $limit: Int) {
  launchpads(offset: $offset, limit: $limit) {
    id
    name
    attempted_launches
    details
    status
    successful_launches
    wikipedia
  }
}